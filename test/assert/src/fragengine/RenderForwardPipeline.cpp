#include<RenderPipeline/RenderPipelineForward.h>
#include<RendererFactory.h>
#include<gtest/gtest.h>

using namespace fragcore;
using namespace fragengine;

TEST(ForwardRenderingPipeline, RenderPipelin_Create_Default_No_Throw){
	// Ref<IRenderer> renderer = Ref<IRenderer>(RenderingFactory::createRendering(RenderingFactory::OpenGL));
	// ASSERT_NO_THROW(RenderPipelineForward(renderer));
}
